name: com.newrelic.elasticsearch
description: Reports status and metrics for elasticsearch service. Legacy file kept for compatibility.
protocol_version: 2
os: windows

commands:
  all:
    command:
      - .\bin\nri-elasticsearch.exe
    prefix: config/elasticsearch
    interval: 15
  inventory:
    command:
      - .\bin\nri-elasticsearch.exe
      - --inventory
    prefix: config/elasticsearch
    interval: 15
